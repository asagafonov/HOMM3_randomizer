import onChange from 'on-change';
import i18next from 'i18next';
import en from './locales/index.js';

i18next.init({
  lng: 'en',
  debug: true,
  resources: {
    en,
  },
});


export default () => {
  const state = {

  };


};
